name: svv-action
channels:
  - conda-forge
dependencies:
  - python=3.10
  - numpy
  - pandas
  - tqdm
  - pip
  - pip:
      - opencv-python
      - ultralytics
      - transformers
      - zensvi
      # Install a PyTorch build appropriate for your CUDA / CPU:
      # See https://pytorch.org/get-started/locally/
      - torch
      - torchvision


